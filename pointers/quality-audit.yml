# Copy to .github/workflows/quality-audit.yml in the consuming repository.
# The all-zeros SHA is a placeholder: README.md, "The pointers".
name: Skill Audit

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    uses: PineappleEmperor/ha-integration-ci/.github/workflows/quality-audit.yml@0000000000000000000000000000000000000000 # v1.0.0
